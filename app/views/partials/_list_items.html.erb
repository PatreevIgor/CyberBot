<h1>Listing Items</h1>
<table>
  <thead>
    <tr>
      <th>class_id</th>
      <th>instance_id</th>
      <th>hash_name</th>
      <th>price</th>
      <th>link</th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.class_id %></td>
        <td><%= item.instance_id %></td>
        <td><%= item.hash_name %></td>
        <td><%= item.price %></td>
        <td><%= link_to 'Link', item.link %></td>
      </tr>
    <% end %>
  </tbody>
</table>
