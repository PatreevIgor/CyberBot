<%= render 'partials/input_form_check_items.html' %>
<p>
<%= render 'partials/new_items_list.html' %>
<p>
<%= form_tag(action_change_status_new_to_main_home_index_path, method: 'post') do %>
  <%= submit_tag('Cange status: new => main') %>
<% end %>
<p>
<%= render 'partials/actually_main_items_list.html' %>
<p>
<%= form_tag(action_update_status_home_index_path, method: 'post') do %>
  <%= submit_tag('Update status') %>
<% end %>
<p>
<%= form_tag(action_create_queries_automatically_buy_item_home_index_path, method: 'post') do %>
  <%= submit_tag('Delete old orders / Create new orders for acctually main items') %>
<% end %>
<p>
<h1>Main Items (actually/not_actually/main) - <%= @all_main_items.size %></h1>
<p>
<%= form_tag(action_remove_ident_items_home_index_path, method: 'post') do %>
  <%= submit_tag('Remove identical items') %>
<% end %>
<p>
<h3>Import data:</h3>
<%= form_tag import_items_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag('Import CSV') %>
<% end %>
<p>
  Download:
  <%= link_to "CSV", items_path(format: "csv") %> |
  <%= link_to "Excel", items_path(format: "xls") %>
</p>


